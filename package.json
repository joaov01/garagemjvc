{
  "name": "garagemjvc",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -R assets/. dist/assets/ && cp -R js dist/ && cp sitemap.xml dist/ && cp robots.txt dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
